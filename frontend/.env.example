# Set this to your deployed Render backend URL for production AI hints
# Leave empty to run frontend-only (AI hints disabled, everything else works)
VITE_API_BASE_URL=https://your-mathquest-backend.onrender.com/api
